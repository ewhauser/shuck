# S001: Refresh SC2086 parity after the initial landing

## Rule

| Field | Value |
|-------|-------|
| Shuck code | S001 |
| ShellCheck code | SC2086 |
| Category | Style |
| Rule file | `crates/shuck-linter/src/rules/style/unquoted_expansion.rs` |

## Current status

`S001` now has a real rule implementation in `crates/shuck-linter/src/rules/style/unquoted_expansion.rs`. The rule walks shared expansion contexts, reports unquoted scalar-expansion spans in command arguments and redirect targets, and suppresses known-safe cases through the new safe-value analysis layer.

Recent local validation:

- focused unit coverage in `crates/shuck-linter/src/rules/style/unquoted_expansion.rs`
- fixture coverage in `crates/shuck-linter/resources/test/fixtures/style/S001.sh`
- `cargo test -p shuck-linter`
- `make check`
- `make test`

The new unit coverage specifically exercises:

- safe scalar bindings such as integers and fixed literals
- recursive binding proofs through aliases, indirect references, arithmetic values, and selected transforms
- cycles and multi-field values that must remain unsafe
- quoted fragments, descriptor-dup targets, and other intentional exclusions

## Stale corpus snapshot

The 2026-04-05 sample that reported 40,176 ShellCheck-only locations is no longer a valid parity snapshot for `S001`. That run predates the implementation and only proved that the rule path was missing at the time.

Until the large-corpus sample is re-run against the current rule, this document should be treated as a post-implementation follow-up note rather than a live parity report.

## Remaining work

The remaining work is to replace the stale pre-implementation corpus numbers with a new sample from the current matcher and then classify the real `ShellCheck-only`, `Shuck-only`, and `location-only` buckets.

## Checklist

- [x] Implement `S001` in a real rule module and wire it into `Checker::check_commands()`.
- [x] Add focused regressions for common unquoted-expansion shapes and intentional exclusions.
- [ ] Re-run the large-corpus comparison and replace the stale pre-implementation report with a real parity analysis.

## Notes

The earlier corpus bootstrap issue still blocks a refreshed large-corpus report in this worktree, so the checklist intentionally leaves the parity-refresh item open.
