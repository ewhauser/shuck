# C014: Current `local`-at-top-level behavior is aligned in the sampled corpus

## Rule

| Field | Value |
|-------|-------|
| Shuck code | C014 |
| ShellCheck code | SC2168 |
| Category | Correctness |
| Rule file | `crates/shuck-linter/src/rules/correctness/script_scope_local.rs` |

## Delta Snapshot

Sampled on 2026-04-05 using `make test-large-corpus SHUCK_LARGE_CORPUS_RULES=C014 SHUCK_LARGE_CORPUS_SAMPLE_PERCENT=10 SHUCK_LARGE_CORPUS_KEEP_GOING=1`.

The run traversed 3,379 supported fixtures, with 223 skipped because the shell was unsupported.

| Bucket | Locations | Scripts |
|--------|-----------|---------|
| ShellCheck-only | 0 | 0 |
| Shuck-only | 0 | 0 |
| Location-only | 0 | 0 |

Additional sample noise:

| Bucket | Scripts |
|--------|---------|
| Parse-abort / environment | 11 |

## Delta Analysis

### Parse-abort fixtures are corpus noise `[environment]`

**Bucket:** Environment  
**Impact:** 11 scripts in the sample

The sampled run did not surface any C014-specific compatibility diffs. The only failures were parse-abort fixtures, including unterminated substitutions, unterminated quotes, a few patch files, and non-shell inputs that the parser already rejects. Those are useful for corpus health tracking, but they do not indicate a mismatch between `local_top_level.rs` and ShellCheck's SC2168 behavior.

**Samples:**
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_ifttt.sh` - unterminated command substitution
- `HariSekhon__DevOps-Bash-tools__git__git_diff_commit.sh` - unterminated double quote
- `google__oss-fuzz__infra__chronos__coverage_test_collection.py` - parser expected `do`

## Checklist

- [ ] Re-run the 10% corpus sample after parser-noise improvements to confirm the zero-delta snapshot stays stable.

## Notes

The current implementation already limits `local` reporting to bash and dash script-scope declarations, so the sampled corpus did not reveal an obvious parity gap for C014.
