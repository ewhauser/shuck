# S002: Close the SC2162 parity gap

## Rule

| Field | Value |
|-------|-------|
| Shuck code | S002 |
| ShellCheck code | SC2162 |
| Category | Style |
| Rule file | `crates/shuck-linter/src/rules/style/read_without_raw.rs` |

## Delta Snapshot

Sampled on 2026-04-05 using `make test-large-corpus SHUCK_LARGE_CORPUS_RULES=S002 SHUCK_LARGE_CORPUS_SAMPLE_PERCENT=10 SHUCK_LARGE_CORPUS_KEEP_GOING=1`.

The run traversed 3,379 supported fixtures, with 223 skipped because the shell was unsupported.

| Bucket | Locations | Scripts |
|--------|-----------|---------|
| ShellCheck-only | 0 | 0 |
| Shuck-only | 3 | 3 |
| Location-only | 0 | 0 |

Additional sample noise:

| Bucket | Scripts |
|--------|---------|
| Parse-abort / environment | 11 |

## Delta Analysis

### Suppressed completion-helper reads are still reported `[shuck-fix]`

**Bucket:** Shuck-only  
**Impact:** 1 location / 1 script in the sample

`Bash-it__bash-it__completion__available__aliases.completion.bash` has an explicit `# shellcheck disable=SC2162` immediately before the `read` site, but Shuck still reports `S002`. ShellCheck stays silent, so this looks like a suppression-path bug rather than a matcher disagreement.

**Sample:**
- `Bash-it__bash-it__completion__available__aliases.completion.bash:38-39` - `# shellcheck disable=SC2162` followed by `while read line; do`

### Project-closure helper reads are still being flagged `[shuck-fix]`

**Bucket:** Shuck-only  
**Impact:** 2 locations / 2 scripts in the sample

The remaining sample hits are in `helmuthdu__aui__sharedfuncs`, which the corpus labels as `project-closure`. Both are interactive `read` calls used in helper logic, and ShellCheck stays silent on them in this slice. Shuck currently reports both sites, which suggests the rule is not respecting the helper-file / closure semantics that the corpus comparison is already surfacing.

**Samples:**
- `helmuthdu__aui__sharedfuncs:296` - `read -p "Confirm video driver: ..."` in interactive video-driver selection
- `helmuthdu__aui__sharedfuncs:591` - `read -e -sn 1 -p "Press enter to continue..."` in an interactive pause helper

### Parse-abort fixtures are corpus noise `[environment]`

**Bucket:** Environment  
**Impact:** 11 scripts in the sample

These fixtures never reached rule comparison because Shuck hit parse errors first. They are useful for corpus health tracking, but they do not indicate a `S002` matcher bug by themselves.

**Samples:**
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_ifttt.sh` - unterminated command substitution
- `HariSekhon__DevOps-Bash-tools__git__git_diff_commit.sh` - unterminated double quote
- `SlackBuildsOrg__slackbuilds__games__njam__patches__drop_gda.patch` - parser expected a word

## Checklist

- [ ] Verify that `# shellcheck disable=SC2162` suppresses `S002` in the linter path.
  - The Bash-it completion helper shows the suppression is currently ignored.
- [ ] Decide whether the `project-closure` helper reads in `helmuthdu__aui__sharedfuncs` are intentional divergences or should be excluded from `S002`.
  - If they are intentional, add a reviewed allowlist entry rather than broadening the matcher.
- [ ] Re-run the 10% corpus sample after the suppression / closure decision.

## Notes

The sample had no ShellCheck-only or location-only mismatches. The current gap is entirely shuck over-reporting in helper and suppression-heavy contexts.
