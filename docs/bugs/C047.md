# C047: Parity check for invalid exit statuses

## Rule

| Field | Value |
|-------|-------|
| Shuck code | C047 |
| ShellCheck code | SC2241 |
| Category | Correctness |
| Rule file | `crates/shuck-linter/src/rules/correctness/invalid_exit_status.rs` |

## Delta Snapshot

Sampled on 2026-04-05 using `make test-large-corpus SHUCK_LARGE_CORPUS_RULES=C047 SHUCK_LARGE_CORPUS_SAMPLE_PERCENT=10 SHUCK_LARGE_CORPUS_KEEP_GOING=1`.

| Bucket | Locations | Scripts |
|--------|-----------|---------|
| ShellCheck-only | 0 | 0 |
| Shuck-only | 0 | 0 |
| Location-only | 0 | 0 |
| Parse-error / environment noise | 11 | 11 |

The sample covered 3,379 supported fixtures and skipped 223 unsupported-shell fixtures.

## Delta Analysis

### Parse and shell-detection noise `[environment]`

**Bucket:** Environment
**Impact:** 11 parse-error fixtures / 11 scripts

The sampled run did not surface any actual ShellCheck-vs-shuck diagnostic deltas for `C047`. The only failures were parse errors and shell-detection artifacts, so they are not actionable as invalid-exit-status parity gaps.

**Samples:**
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_ifttt.sh` - unterminated command substitution
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_telegram.sh` - unterminated command substitution
- `HariSekhon__DevOps-Bash-tools__git__git_diff_commit.sh` - unterminated double quote
- `google__oss-fuzz__infra__chronos__coverage_test_collection.py` - parse error, expected `do`
- `oh-my-fish__oh-my-fish__tools__generate-authors.fish` - parse error, expected `{` or `(`

## Checklist

- [ ] Re-run the 10% corpus sample if parser or shell-detection behavior changes.

## Notes

`C047` looked parity-clean in the sampled corpus. No ShellCheck-only, shuck-only, or location-only differences were observed in the data returned by the 10% sample.
