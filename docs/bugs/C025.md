# C025: Positional parameter braces

## Rule

| Field | Value |
|-------|-------|
| Shuck code | C025 |
| ShellCheck code | SC1037 |
| Category | Correctness |
| Rule file | `crates/shuck-linter/src/rules/correctness/positional_ten_braces.rs` |

## Delta Snapshot

Sampled on 2026-04-05 using:

```bash
make test-large-corpus SHUCK_LARGE_CORPUS_RULES=C025 SHUCK_LARGE_CORPUS_SAMPLE_PERCENT=10 SHUCK_LARGE_CORPUS_KEEP_GOING=1
```

The sampled run processed all 3,379 supported fixtures and skipped 223 unsupported-shell fixtures. It reported 11 parse/environment fixtures, but it did not emit any actual ShellCheck-vs-shuck diagnostic diff blocks for `C025`.

| Bucket | Locations | Scripts |
|--------|-----------:|--------:|
| ShellCheck-only | 0 | 0 |
| Shuck-only | 0 | 0 |
| Location-only | 0 | 0 |
| Parse-error / environment noise | 11 | 11 |

## Delta Analysis

### Parse and shell-detection noise `[environment]`

**Bucket:** Environment
**Impact:** 11 fixtures / 11 scripts

The sampled corpus did not surface any rule-level disagreements for `C025`. The only reported failures were parser errors or shell-detection artifacts, so they are not actionable as positional-parameter parity gaps.

**Samples:**
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_ifttt.sh` - shuck parse error
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_telegram.sh` - shuck parse error
- `HariSekhon__DevOps-Bash-tools__git__git_diff_commit.sh` - shuck parse error
- `SlackBuildsOrg__slackbuilds__games__njam__patches__drop_gda.patch` - shuck parse error
- `oh-my-fish__oh-my-fish__tools__generate-authors.fish` - shuck parse error

## Checklist

- [ ] Re-run the 10% corpus sample for `C025` after any future parser or shell-detection changes and refresh the counts in this document.

## Notes

This report is intentionally minimal because the sampled corpus did not show a conformance gap for `C025`. Based on the emitted output, I infer that `C025` is currently parity-clean in the sampled set and that the visible failures are environmental noise rather than rule discrepancies.
