# C046: Reconcile the SC2124 comparison target

## Rule

| Field | Value |
|-------|-------|
| Shuck code | C046 |
| ShellCheck code | SC2124 |
| Category | Correctness |
| Rule file | `crates/shuck-linter/src/rules/correctness/pipe_to_kill.rs` |

## Delta snapshot

Sampled on 2026-04-05 using `make test-large-corpus SHUCK_LARGE_CORPUS_RULES=C046 SHUCK_LARGE_CORPUS_SAMPLE_PERCENT=10 SHUCK_LARGE_CORPUS_KEEP_GOING=1`.

The sample covered 3,602 fixtures in total and skipped 223 unsupported-shell fixtures.

| Bucket | Locations | Scripts |
|--------|-----------|---------|
| ShellCheck-only | 11 | 11 |
| Shuck-only | 0 | 0 |
| Location-only | 0 | 0 |
| Environment | 11 | 11 |

The sampled corpus did not surface any obvious pipe-to-`kill` mismatch. Instead, the rule target is dominated by unrelated `SC2124` array-to-scalar warnings in helper-heavy scripts, which suggests the current comparison slice is not exercising the intended `C046` shape.

## Delta analysis

### Array-to-scalar assignment warnings in helper scripts `[environment]`

**Bucket:** ShellCheck-only
**Impact:** 11 locations / 11 scripts

Every sampled ShellCheck-only hit was an `SC2124` warning about array content being forced into a scalar context. These are real ShellCheck diagnostics, but they do not resemble the pipe-to-`kill` behavior described by `C046`'s fixture and implementation. In other words, the run is picking up unrelated corpus noise rather than a meaningful `C046` semantic gap.

**Samples:**
- `RetroPie__RetroPie-Setup__scriptmodules__supplementary__wifi.sh:287` - command output captured into `choice` and then indexed as if it were a scalar
- `SlackBuildsOrg__slackbuilds__office__smoffice2021__presentations21:3` - an argument slice is fed into a scalar assignment path
- `SlackBuildsOrg__slackbuilds__system__postgresql__rc.postgresql.new:64` - command text is folded into a scalar shell variable
- `basecamp__omarchy__bin__omarchy-launch-or-focus-webapp:13` - array-style command composition is assigned into a scalar variable
- `moovweb__gvm__examples__native__configure:3146` - generated configure-script state is assigned through an array-like expansion
- `rvm__rvm__scripts__alias:106` - alias metadata is expanded into a scalar context
- `void-linux__void-packages__common__environment__setup__archive.sh:178` - array contents are flattened into a scalar assignment

### Parser aborts on unsupported fixtures `[environment]`

**Bucket:** Environment
**Impact:** 11 fixtures / 11 scripts

The remaining failures are parser aborts on patch files, fish scripts, and malformed shell snippets. They do not help distinguish whether `C046` itself is behaving correctly.

**Samples:**
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_ifttt.sh` - unterminated command substitution
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_telegram.sh` - unterminated command substitution
- `HariSekhon__DevOps-Bash-tools__git__git_diff_commit.sh` - unterminated double quote
- `SlackBuildsOrg__slackbuilds__games__njam__patches__drop_gda.patch` - patch content is not shell syntax
- `SlackBuildsOrg__slackbuilds__network__redir__patches__02_use_ntohs.dpatch` - patch content is not shell syntax
- `alpinelinux__aports__main__docbook2x__04_fix_442782_preprocessor_declaration_syntax.patch` - patch content is not shell syntax
- `google__oss-fuzz__infra__chronos__coverage_test_collection.py` - non-shell fixture
- `oh-my-fish__oh-my-fish__tools__generate-authors.fish` - fish syntax rather than bash-style shell syntax
- `scop__bash-completion__completions-core__ssh-keygen.bash` - parse failure in a case pattern
- `termux__termux-packages__packages__tsocks__03_fixloop.patch` - patch content is not shell syntax
- `void-linux__void-packages__srcpkgs__nvi__patches__nvi-08-lfs.patch` - patch content is not shell syntax

## Checklist

- [ ] Confirm whether `C046` is meant to compare against the current `SC2124` target or whether the mapping should be audited, because the corpus sample is surfacing array/scalar warnings rather than pipe-to-`kill` cases.
  - Initiative 12 now records this as a comparison-target note in `crates/shuck/tests/testdata/corpus-metadata/c046.yaml`.
- [ ] Re-run the large-corpus comparison after the mapping question is resolved or after adding more direct pipe-to-`kill` fixtures.

## Notes

`crates/shuck-linter/resources/test/fixtures/correctness/C046.sh` still captures the intended pipe-to-`kill` scenario, so the implemented rule itself appears to be focused. The large-corpus run just does not surface that shape yet, which makes the current sample more useful as a mapping/noise check than as a semantic parity check.
