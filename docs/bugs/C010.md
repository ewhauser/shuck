# C010: Collapse mixed short-circuit parity onto one rule

## Rule

| Field | Value |
|-------|-------|
| Shuck code | C010 |
| ShellCheck code | SC2015 primary, SC2296 alias |
| Category | Correctness |
| Rule file | `crates/shuck-linter/src/rules/correctness/chained_test_branches.rs` |

## Summary

The previous split between `C010` and `C079` matched two internal fact kinds, but it left the large-corpus harness comparing one live ShellCheck warning family against two Shuck rules. The refactor removes that user-facing split.

`C010` now owns both:

- condition-only shortcut chains
- broader `A && B || C` fallthrough chains after the existing exemptions

## Resolution

Keep the internal fact classification, but report both shapes as `C010`. Accept both `SC2015` and `SC2296` as compatibility codes for the same rule and retire the separate `C079` rule surface.

With the fallback exemptions aligned to the nix-pinned ShellCheck oracle, the targeted 10% corpus sample for `C010` now completes with zero reviewed divergences and no comparison metadata file.

## Checklist

- [x] Fold fallthrough reporting into `C010`.
- [x] Retire the separate `C079` rule entry and remap its compatibility aliases.
- [x] Re-run the targeted and large-corpus parity checks after the refactor.

## Notes

The internal facts still distinguish condition-only chains from fallthrough chains. The change is only to the user-facing and corpus-comparison surface.
