# C050: Review arithmetic redirection targets

## Rule

| Field | Value |
|-------|-------|
| Shuck code | C050 |
| ShellCheck code | SC2244 |
| Category | Correctness |
| Rule file | `crates/shuck-linter/src/rules/correctness/arithmetic_redirection_target.rs` |

## Delta Snapshot

Sampled on 2026-04-05 using `make test-large-corpus SHUCK_LARGE_CORPUS_RULES=C050 SHUCK_LARGE_CORPUS_SAMPLE_PERCENT=10 SHUCK_LARGE_CORPUS_KEEP_GOING=1`.

| Bucket | Locations | Scripts |
|--------|-----------:|--------:|
| ShellCheck-only | 0 | 0 |
| Shuck-only | 4 | 3 |
| Location-only | 0 | 0 |
| Parse-error / environment noise | 11 | 11 |

The sample covered 3,379 supported fixtures and skipped 223 unsupported-shell fixtures.

## Delta Analysis

### Generated redirection targets `[shuck-correct]`

**Bucket:** Shuck-only  
**Impact:** 4 locations / 3 scripts

Shuck reports arithmetic expansions used as redirection targets in a few generated or project-closure-heavy scripts. ShellCheck stayed silent on the sampled `SC2244` locations, but the redirection target is still being computed with `$((...))`, which matches the rule's intent. These look more like intentional Shuck-side coverage than a narrow implementation bug.

**Samples:**
- `bittorf__kalua__openwrt-addons__etc__kalua_init.user:107:1-144:1` - arithmetic target in a long function block.
- `docker-library__official-images__test__tests__redis-basics__run.sh:21:1-47:1` - arithmetic target inside a generated test harness.
- `moovweb__gvm__examples__native__configure:11431:1-11797:1` - generated configure output with an arithmetic target.
- `moovweb__gvm__examples__native__configure:12645:1-13418:1` - second arithmetic target in the same generated script.

### Parse and shell-detection noise `[environment]`

**Bucket:** Environment  
**Impact:** 11 parse-error fixtures / 11 scripts

The rest of the sample is parser failure or shell-detection noise, so it is not actionable as a `C050` conformance gap.

**Samples:**
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_ifttt.sh` - unterminated command substitution.
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_telegram.sh` - unterminated command substitution.
- `HariSekhon__DevOps-Bash-tools__git__git_diff_commit.sh` - unterminated double quote.
- `google__oss-fuzz__infra__chronos__coverage_test_collection.py` - parse error, expected `do`.
- `oh-my-fish__oh-my-fish__tools__generate-authors.fish` - parse error, expected `{` or `(`.

## Checklist

No shuck-fix items were identified in this sample.

## Notes

A minimal ShellCheck run on `echo hi > "$((i++))"` reports `SC2257`, not `SC2244`, so the current YAML mapping may not line up with ShellCheck's closest warning on the canonical example. I did not change the mapping here because this task was limited to `docs/bugs/C050.md`.
