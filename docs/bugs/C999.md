# C999: Placeholder rule has no conformance target yet

## Rule

| Field | Value |
|-------|-------|
| Shuck code | C999 |
| ShellCheck code | none |
| Category | Correctness |
| Rule file | `crates/shuck-linter/src/rules/correctness/noop.rs` |

## Status snapshot

Reviewed on 2026-04-05 without running `make test-large-corpus`.

`C999` is a temporary placeholder rule used to keep the dispatch pipeline wired up. It does not have a `docs/rules/C999.yaml` definition, no mapped ShellCheck code, and no real lint implementation beyond a no-op function.

## Why there is no conformance run

The normal conformance workflow for this repository starts by resolving a rule YAML entry and its mapped ShellCheck code, then compares the large corpus against that external oracle. `C999` cannot go through that workflow because the prerequisites do not exist yet:

- there is no `docs/rules/C999.yaml`
- there is no mapped ShellCheck rule to compare against
- `crates/shuck-linter/src/rules/correctness/noop.rs` is explicitly marked as a temporary placeholder

## Checklist

- [ ] Replace the placeholder with a real rule or remove it from the public registry.
- [ ] Add a `docs/rules/C999.yaml` definition if the code is meant to stay.
- [ ] Add a ShellCheck mapping or document that the rule is intentionally Shuck-only.
- [ ] Re-run conformance and replace this placeholder report once the rule has a real implementation target.

## Notes

This file exists so every currently registered rule has a corresponding report in `docs/bugs/`. Unlike the other reports, it documents a missing rule definition rather than a corpus-derived parity gap.
