# C011: Track the SC2013 parity gap

## Rule

| Field | Value |
|-------|-------|
| Shuck code | C011 |
| ShellCheck code | SC2013 |
| Category | Correctness |
| Rule file | `crates/shuck-linter/src/rules/correctness/line_oriented_input.rs` |

## Delta Snapshot

Sampled on 2026-04-05 using:

```bash
make test-large-corpus SHUCK_LARGE_CORPUS_RULES=C011 SHUCK_LARGE_CORPUS_SAMPLE_PERCENT=10 SHUCK_LARGE_CORPUS_KEEP_GOING=1
```

| Bucket | Locations | Scripts |
|--------|-----------:|--------:|
| ShellCheck-only | 0 | 0 |
| Shuck-only | 392 | 341 |
| Location-only | 0 | 0 |
| Parse-error / environment noise | 11 | 11 |

The sample covered 3,379 supported fixtures and skipped 223 unsupported-shell fixtures.

## Delta Analysis

### Command-output `for` loops `[shuck-correct]`

**Bucket:** Shuck-only  
**Impact:** The entire sampled compatibility signal

The consistent shape in this sample is a `for` loop iterating over command output, usually via `$(...)`. ShellCheck stays silent on these examples, but shuck flags them as line-oriented input hazards. The scripts look like real correctness issues rather than false positives: the shell will split command output on whitespace, which is exactly the behavior this rule is meant to catch.

**Samples:**
- `GameServerManagers__LinuxGSM__.github__workflows__serverlist-validate.sh:28` - `for shortname in $(tail -n +2 serverlist.csv | cut -d ',' -f1); do`
- `HariSekhon__DevOps-Bash-tools__checks__check_no_tabs.sh:34` - `for filename in $(find "${1:-.}" -type f | grep -Evf ... | sort); do`
- `SlackBuildsOrg__slackbuilds__games__redeclipse__redeclipse.SlackBuild:79` - `for SUBMODULE in $( cat $CWD/submodules-list ); do`
- `SlackBuildsOrg__slackbuilds__system__dlm__dlm.SlackBuild:83` - `for i in $(cat $CWD/patches/series) ; do`
- `rvm__rvm__scripts__snapshot:71` - `for snapshot_gemset in $(__rvm_list_gemset_strings)`

### Project-Closure / Directive Noise `[environment]`

**Bucket:** Shuck-only  
**Impact:** 29 labeled compatibility-diff scripts in this sample

Some of the sample scripts are already known to be noisy because they depend on repository-local helper files, top-level directives, or shell-detection edge cases. Those labels do not change the basic result here, but they are worth keeping separate from the core `C011` signal so they do not get mistaken for a distinct rule bug.

**Samples:**
- `HariSekhon__DevOps-Bash-tools__checks__check_no_tabs.sh:34` - `directive-handling`, `project-closure`
- `HariSekhon__DevOps-Bash-tools__gcp__gcp_iam_roles_granted_to_identity.sh:85` - `directive-handling`, `project-closure`
- `RetroPie__RetroPie-Setup__scriptmodules__supplementary__runcommand__runcommand.sh:733` - `project-closure`
- `openrc__openrc__sh__rc-functions.sh:111` - `unknown-shell-collapse`
- `termux__termux-packages__packages__rust__build.sh:258` - `unknown-shell-collapse`

### Parse Errors `[environment]`

**Bucket:** Environment noise  
**Impact:** 11 fixtures / 11 scripts

These fixtures are parser failures or unsupported-shell artifacts, so they are not actionable parity evidence for `C011`.

**Samples:**
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_ifttt.sh`
- `GameServerManagers__LinuxGSM__lgsm__modules__alert_telegram.sh`
- `HariSekhon__DevOps-Bash-tools__git__git_diff_commit.sh`
- `oh-my-fish__oh-my-fish__tools__generate-authors.fish`

## Checklist

No `shuck-fix` items were identified in this sample. The observed differences are either reviewed `shuck-correct` behavior or environment noise.

## Allowlisted Divergences

None were added in this pass.

## Notes

This sample suggests `C011` is not a small parity gap. The meaningful signal is that shuck catches direct command-output `for` loops consistently, while the remaining variance is mostly corpus environment noise rather than a narrow implementation bug.
