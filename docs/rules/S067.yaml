legacy_code: SH-294
legacy_name: backtick-output-to-command
new_category: Style
new_code: S067
runtime_kind: ast
shellcheck_code: SC2063
shells:
  - sh
  - bash
  - dash
  - ksh
description: Backtick output is word-split and passed as arguments to a command.
rationale: "Use `xargs` or a `while read` loop for safe argument passing."
source:
  shell_checks_rule: rules/SH-294.yaml
  shell_checks_example: examples/294.sh
examples:
  - kind: invalid
    source: shell-checks/examples/294.sh
    code: "#!/bin/sh\n# shellcheck disable=2006,2046\ngit branch -d `git branch --merged | grep -v '^*' | grep -v 'master' | tr -d '\n'`\n"
