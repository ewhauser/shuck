legacy_code: SH-112
legacy_name: else-if
new_category: Correctness
new_code: C038
runtime_kind: ast
shellcheck_code: SC1064
shells:
  - sh
  - bash
  - dash
  - ksh
description: "`else if` is used where bash expects `elif`."
rationale: "Collapse the nested form into a single `elif` branch."
source:
  shell_checks_rule: rules/SH-112.yaml
  shell_checks_example: examples/112.sh
examples:
  - kind: invalid
    source: shell-checks/examples/112.sh
    code: |
      #!/bin/bash
      if true; then
        :
      else if true; then
        :
      fi
      fi
