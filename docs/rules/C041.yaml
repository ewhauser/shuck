legacy_code: SH-121
legacy_name: c-style-comment
new_category: Correctness
new_code: C041
runtime_kind: ast
shellcheck_code: SC1127
shells:
  - sh
  - bash
  - dash
  - ksh
description: C-style comment text is not a shell comment.
rationale: "Use `#` comments in shell scripts instead of `/* ... */`."
source:
  shell_checks_rule: rules/SH-121.yaml
  shell_checks_example: examples/121.sh
examples:
  - kind: invalid
    source: shell-checks/examples/121.sh
    code: |
      #!/bin/sh
      /* note */
