legacy_code: SH-253
legacy_name: find-output-in-loop
new_category: Correctness
new_code: C113
runtime_kind: ast
shells:
  - sh
  - bash
  - dash
  - ksh
description: "A `find` command output is captured in a word-splitting loop."
rationale: "Use `find -exec` or `-print0 | while read` for safe file handling."
source:
  shell_checks_rule: rules/SH-253.yaml
  shell_checks_example: examples/253.sh
examples:
  - kind: invalid
    source: shell-checks/examples/253.sh
    code: |
      #!/bin/sh
      # shellcheck disable=2086,2154,2044
      for file in $( find 2>/dev/null "$dir" -type f -name '[0-9]*' ); do echo "$file"; done
