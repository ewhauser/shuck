legacy_code: SH-216
legacy_name: quoted-command-in-test
new_category: Correctness
new_code: C089
runtime_kind: ast
shellcheck_code: SC2311
shells:
  - sh
  - bash
  - dash
  - ksh
description: A command pipeline is quoted as a string inside a test bracket.
rationale: Remove the quotes and use command substitution or a variable.
source:
  shell_checks_rule: rules/SH-216.yaml
  shell_checks_example: examples/216.sh
examples:
  - kind: invalid
    source: shell-checks/examples/216.sh
    code: |
      #!/bin/sh
      if [ "lsmod | grep v4l2loopback" ]; then echo loaded; fi
