legacy_code: SH-110
legacy_name: broken-test-parse
new_category: Correctness
new_code: C037
runtime_kind: ast
shellcheck_code: SC1074
shells:
  - sh
  - bash
  - dash
  - ksh
description: "A malformed `[` test expression cannot be parsed by ShellCheck."
rationale: Correct the test syntax before relying on any other static checks.
source:
  shell_checks_rule: rules/SH-110.yaml
  shell_checks_example: examples/110.sh
examples:
  - kind: invalid
    source: shell-checks/examples/110.sh
    code: |
      #!/bin/sh
      # shellcheck disable=1009,1072
      if [ x = y; then :; fi
