legacy_code: SH-286
legacy_name: zsh-parameter-flag
new_category: Portability
new_code: X076
runtime_kind: ast
shellcheck_code: SC2300
shellcheck_level: error
shells:
  - sh
  - bash
  - dash
  - ksh
description: A parameter operator is applied directly to a command substitution.
rationale: Store the command output in a variable before slicing, defaulting, or replacing it.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-286.yaml
  shell_checks_example: examples/286.sh
examples:
  - kind: invalid
    source: shell-checks/examples/286.sh
    code: |
      #!/bin/sh
      # shellcheck disable=2154,2034,2082,3057,2299
      x="${$(svn info):0:1}"
