legacy_code: SH-101
legacy_name: extended-glob-in-test
new_category: Portability
new_code: X034
runtime_kind: ast
shellcheck_code: SC2221
shells:
  - sh
  - bash
  - dash
  - ksh
description: "A `[[` match uses extended glob syntax."
rationale: "Switch to `case` or a portable pattern."
source:
  shell_checks_rule: rules/SH-101.yaml
  shell_checks_example: examples/101.sh
examples:
  - kind: invalid
    source: shell-checks/examples/101.sh
    code: |
      #!/bin/sh
      if [[ $term == (xterm|screen)* ]]; then
        :
      fi
