legacy_code: SH-236
legacy_name: glob-in-test-directory
new_category: Correctness
new_code: C102
runtime_kind: ast
shellcheck_code: SC2331
shells:
  - sh
  - bash
  - dash
description: "A glob pattern inside a `[ -d ... ]` test may match multiple paths."
rationale: Quote the path or use a loop to test each match.
source:
  shell_checks_rule: rules/SH-236.yaml
  shell_checks_example: examples/236.sh
examples:
  - kind: invalid
    source: shell-checks/examples/236.sh
    code: |
      #!/bin/bash
      # shellcheck disable=2154
      if [ -d mtp2* ]; then echo found; fi
