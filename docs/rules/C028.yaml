legacy_code: SH-094
legacy_name: spaced-assignment
new_category: Correctness
new_code: C028
runtime_kind: ast
shellcheck_code: SC1066
shells:
  - sh
  - bash
  - dash
  - ksh
description: "An assignment is split by a space after `=`, so the shell treats it as a command name and argument instead."
rationale: Remove the space so the shell parses it as a real assignment.
source:
  shell_checks_rule: rules/SH-094.yaml
  shell_checks_example: examples/094.sh
examples:
  - kind: invalid
    source: shell-checks/examples/094.sh
    code: |
      foo= bar
