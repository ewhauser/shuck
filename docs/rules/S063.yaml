legacy_code: SH-252
legacy_name: relative-symlink-target
new_category: Style
new_code: S063
runtime_kind: ast
shellcheck_code: SC2347
shells:
  - sh
  - bash
  - dash
  - ksh
description: "A symbolic link target uses a relative path with many `../` segments."
rationale: Use an absolute path or verify the relative target resolves correctly.
source:
  shell_checks_rule: rules/SH-252.yaml
  shell_checks_example: examples/252.sh
examples:
  - kind: invalid
    source: shell-checks/examples/252.sh
    code: |
      #!/bin/bash
      ln -s ../../share/doc/guide.pdf
