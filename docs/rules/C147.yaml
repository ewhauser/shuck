legacy_code: SH-336
legacy_name: keyword-function-name
new_category: Correctness
new_code: C147
runtime_kind: ast
shellcheck_code: SC2113
shells:
  - sh
  - dash
description: The `function` keyword is used without trailing `()` in a `sh`-style script.
rationale: Define shell functions with the plain `name() { ...; }` form instead.
source:
  shell_checks_rule: rules/SH-336.yaml
  shell_checks_example: examples/336.sh
examples:
  - kind: invalid
    source: shell-checks/examples/336.sh
    code: |
      #!/bin/sh
      # shellcheck disable=1072,1073
      for() { :; }
