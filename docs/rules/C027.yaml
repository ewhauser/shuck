legacy_code: SH-091
legacy_name: bare-done-word
new_category: Correctness
new_code: C027
runtime_kind: ast
shellcheck_code: SC1010
shells:
  - sh
  - bash
  - dash
  - ksh
description: A reserved word is being passed as a literal argument.
rationale: Quote it if it is text, or place it where the shell expects a keyword.
source:
  shell_checks_rule: rules/SH-091.yaml
  shell_checks_example: examples/091.sh
examples:
  - kind: invalid
    source: shell-checks/examples/091.sh
    code: |
      #!/bin/sh
      printf '%s
      ' done
