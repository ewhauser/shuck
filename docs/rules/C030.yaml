legacy_code: SH-098
legacy_name: missing-bracket-space
new_category: Correctness
new_code: C030
runtime_kind: ast
shellcheck_code: SC1020
shells:
  - sh
  - bash
  - dash
  - ksh
description: A test bracket is closed without surrounding space.
rationale: "Keep spaces around `[` and `]` in test expressions."
source:
  shell_checks_rule: rules/SH-098.yaml
  shell_checks_example: examples/098.sh
examples:
  - kind: invalid
    source: shell-checks/examples/098.sh
    code: |
      #!/bin/bash
      if [ -d /tmp]; then
        :
      fi
