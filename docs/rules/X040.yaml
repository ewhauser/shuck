legacy_code: SH-126
legacy_name: array-subscript-test
new_category: Portability
new_code: X040
runtime_kind: ast
shellcheck_code: SC2202
shellcheck_level: warning
shells:
  - sh
  - bash
  - dash
  - ksh
description: Uses bare array-style subscripting in a POSIX shell test
rationale: Use a portable test or a shell that actually supports arrays.
safe_fix: false
fix_description: null
source:
  shell_checks_rule: rules/SH-126.yaml
  shell_checks_example: examples/126.sh
examples:
  - kind: invalid
    source: shell-checks/examples/126.sh
    code: |
      #!/bin/sh
      if [ $tools[kops] ]; then
        :
      fi
