legacy_code: SH-310
legacy_name: misspelled-option-name
new_category: Correctness
new_code: C132
runtime_kind: ast
shellcheck_code: SC2380
shells:
  - sh
  - bash
  - dash
  - ksh
description: A configure option name looks like a misspelling.
rationale: Check the option name against the expected spelling.
source:
  shell_checks_rule: rules/SH-310.yaml
  shell_checks_example: examples/310.sh
examples:
  - kind: invalid
    source: shell-checks/examples/310.sh
    code: |
      #!/bin/bash
      # shellcheck disable=2154,2086,2097
      CFLAGS="${SLKCFLAGS}" \
      ./configure \
        --with-optmizer=${CFLAGS}
