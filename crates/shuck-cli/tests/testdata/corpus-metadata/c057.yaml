reviewed_divergences:
  - side: shuck-only
    path_suffix: "bittorf__kalua__openwrt-addons__etc__kalua__watch"
    line: 1336
    reason: "This helper wrapper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
  - side: shuck-only
    path_suffix: "nvm-sh__nvm__nvm-exec"
    line: 13
    reason: "This helper wrapper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
  - side: shuck-only
    path_suffix: "nvm-sh__nvm__nvm.sh"
    line: 3573
    reason: "This helper wrapper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
  - side: shuck-only
    path_suffix: "nvm-sh__nvm__nvm.sh"
    line: 4008
    reason: "This helper wrapper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
  - side: shuck-only
    path_suffix: "nvm-sh__nvm__nvm.sh"
    line: 4141
    reason: "This helper wrapper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
  - side: shuck-only
    path_suffix: "nvm-sh__nvm__nvm.sh"
    line: 4158
    reason: "This helper wrapper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
  - side: shuck-only
    path_suffix: "nvm-sh__nvm__nvm.sh"
    line: 4223
    reason: "This helper wrapper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
  - side: shuck-only
    path_suffix: "nvm-sh__nvm__nvm.sh"
    line: 4354
    reason: "This helper wrapper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
  - side: shuck-only
    path_suffix: "nvm-sh__nvm__test__fast__Running 'nvm use' should drop CR char automatically"
    line: 17
    reason: "This test helper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
  - side: shuck-only
    path_suffix: "nvm-sh__nvm__test__fast__Running 'nvm use' should drop CR char automatically"
    line: 21
    reason: "This test helper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
  - side: shuck-only
    path_suffix: "nvm-sh__nvm__test__fast__Running 'nvm use' should drop CR char automatically"
    line: 27
    reason: "This test helper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
  - side: shuck-only
    path_suffix: "termux__termux-packages__scripts__updates__utils__termux_pkg_is_update_needed.sh"
    line: 14
    reason: "This project helper redirects command-substitution stdout away inside the subshell, and Shuck intentionally keeps the dedicated warning even though the current oracle stays silent on that wrapper form."
