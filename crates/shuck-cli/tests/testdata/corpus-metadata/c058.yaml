reviewed_divergences:
  - side: shuck-only
    path_contains: "CISOfy__lynis__"
    reason: "C058 intentionally flags command substitutions that discard or reroute stdout inside the subshell, even when they appear in helper wrappers, project-closure scripts, or test harnesses. ShellCheck stays silent on these SC2256 cases, so these repo-family hits remain a stricter Shuck policy without hiding a distinct implementation bug."
  - side: shuck-only
    path_contains: "HariSekhon__DevOps-Bash-tools__"
    reason: "C058 intentionally flags command substitutions that discard or reroute stdout inside the subshell, even when they appear in helper wrappers, project-closure scripts, or test harnesses. ShellCheck stays silent on these SC2256 cases, so these repo-family hits remain a stricter Shuck policy without hiding a distinct implementation bug."
  - side: shuck-only
    path_contains: "SlackBuildsOrg__slackbuilds__"
    reason: "C058 intentionally flags command substitutions that discard or reroute stdout inside the subshell, even when they appear in helper wrappers, project-closure scripts, or test harnesses. ShellCheck stays silent on these SC2256 cases, so these repo-family hits remain a stricter Shuck policy without hiding a distinct implementation bug."
  - side: shuck-only
    path_contains: "mathiasbynens__dotfiles__"
    reason: "C058 intentionally flags command substitutions that discard or reroute stdout inside the subshell, even when they appear in helper wrappers, project-closure scripts, or test harnesses. ShellCheck stays silent on these SC2256 cases, so these repo-family hits remain a stricter Shuck policy without hiding a distinct implementation bug."
  - side: shuck-only
    path_contains: "nvm-sh__nvm__"
    reason: "C058 intentionally flags command substitutions that discard or reroute stdout inside the subshell, even when they appear in helper wrappers, project-closure scripts, or test harnesses. ShellCheck stays silent on these SC2256 cases, so these repo-family hits remain a stricter Shuck policy without hiding a distinct implementation bug."
  - side: shuck-only
    path_contains: "termux__termux-packages__"
    reason: "C058 intentionally flags command substitutions that discard or reroute stdout inside the subshell, even when they appear in helper wrappers, project-closure scripts, or test harnesses. ShellCheck stays silent on these SC2256 cases, so these repo-family hits remain a stricter Shuck policy without hiding a distinct implementation bug."
  - side: shuck-only
    path_contains: "tj__git-extras__"
    reason: "C058 intentionally flags command substitutions that discard or reroute stdout inside the subshell, even when they appear in helper wrappers, project-closure scripts, or test harnesses. ShellCheck stays silent on these SC2256 cases, so these repo-family hits remain a stricter Shuck policy without hiding a distinct implementation bug."
  - side: shuck-only
    path_contains: "tteck__Proxmox__"
    reason: "C058 intentionally flags command substitutions that discard or reroute stdout inside the subshell, even when they appear in helper wrappers, project-closure scripts, or test harnesses. ShellCheck stays silent on these SC2256 cases, so these repo-family hits remain a stricter Shuck policy without hiding a distinct implementation bug."
