reviewed_divergences:
  - side: shuck-only
    path_contains: "Bash-it__bash-it__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "GameServerManagers__LinuxGSM__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "RetroPie__RetroPie-Setup__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "SlackBuildsOrg__slackbuilds__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "aristocratos__bashtop__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "basecamp__omarchy__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "bitnami__containers__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "community-scripts__ProxmoxVE__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "docker-mailserver__docker-mailserver__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "dokku__dokku__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "dylanaraps__neofetch__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "gentoo__gentoo__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "google__oss-fuzz__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "itzg__docker-minecraft-server__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "jessfraz__dotfiles__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "leebaird__discover__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "pi-hole__pi-hole__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "super-linter__super-linter__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "termux__termux-packages__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "tj__git-extras__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "tteck__Proxmox__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "v1s1t0r1sh3r3__airgeddon__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so these repo-family hits remain a stricter Shuck policy check."
  - side: shuck-only
    path_contains: "void-linux__void-packages__"
    reason: "C109 intentionally flags `mapfile`/`readarray` reads from process substitutions. In the current large-corpus oracle environment, ShellCheck 0.11.0 does not emit SC2339 for these patterns, so this rule remains a stricter Shuck policy check."
