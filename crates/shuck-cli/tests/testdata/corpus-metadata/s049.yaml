reviewed_divergences:
  - side: shellcheck-only
    path_suffix: "acmesh-official__acme.sh__acme.sh"
    reason: "This helper still produces ShellCheck's bracketed-`tr` warning at a few project-local sites in the corpus harness, while Shuck's current S049 implementation misses those exact locations, so we keep this file reviewed narrowly instead of broadening the rule metadata."
  - side: shellcheck-only
    path_suffix: "romkatv__powerlevel10k__gitstatus__build"
    reason: "This build helper still surfaces ShellCheck's bracketed-`tr` warning in the corpus run, while Shuck misses the exact nested pipeline sites here, so the remaining file stays reviewed narrowly."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__functions__detect__system_name__lsb_release"
    reason: "This release-detection helper still produces ShellCheck's bracketed-`tr` warning in the corpus harness, while Shuck misses this exact helper-local site, so we keep the file reviewed narrowly."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__functions__detect__system_name__os_release"
    reason: "This release-detection helper still produces ShellCheck's bracketed-`tr` warning in the corpus harness, while Shuck misses this exact helper-local site, so we keep the file reviewed narrowly."
  - side: shellcheck-only
    path_suffix: "void-linux__void-packages__srcpkgs__brother-dcp197c-cupswrapper__files__cupswrapper.void"
    reason: "This package wrapper still surfaces ShellCheck's bracketed-`tr` warning in the corpus run, while Shuck misses the exact backtick pipeline site here, so the remaining file stays reviewed narrowly."
  - side: shuck-only
    path_suffix: "pyenv__pyenv__plugins__python-build__bin__python-build"
    reason: "This helper still lands on ShellCheck 0.11.0's older SC2060 side of the bracketed-`tr` warning family, while S049 now compares via the current SC2021-shaped behavior, so the remaining file stays reviewed narrowly."
