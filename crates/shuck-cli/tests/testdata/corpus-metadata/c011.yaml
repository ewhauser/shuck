reviewed_divergences:
  - side: shuck-only
    path_contains: "233boy__v2ray__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "89luca89__distrobox__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "Bash-it__bash-it__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "CISOfy__lynis__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "GameServerManagers__LinuxGSM__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "HariSekhon__DevOps-Bash-tools__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "RetroPie__RetroPie-Setup__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "SlackBuildsOrg__slackbuilds__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "acmesh-official__acme.sh__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "alpinelinux__aports__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "angristan__openvpn-install__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "aristocratos__bashtop__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "asdf-vm__asdf__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "awslabs__git-secrets__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "basecamp__omarchy__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "bin456789__reinstall__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "bitnami__containers__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "bittorf__kalua__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "community-scripts__ProxmoxVE__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "dehydrated-io__dehydrated__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "docker-library__official-images__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "docker-mailserver__docker-mailserver__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "docker__docker-bench-security__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "dockur__windows__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "dokku__dokku__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "fideloper__Vaprobash__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "gentoo__gentoo__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "google__oss-fuzz__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "holman__dotfiles__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "jessfraz__dotfiles__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "juewuy__ShellCrash__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "kward__shunit2__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "leebaird__discover__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "masonr__yet-another-bench-script__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "moovweb__gvm__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "nvm-sh__nvm__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "ohmyzsh__ohmyzsh__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "openrc__openrc__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "p8952__bocker__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "pi-hole__pi-hole__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "pyenv__pyenv__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "pystardust__ani-cli__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "quickemu-project__quickemu__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "rbenv__rbenv__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "rvm__rvm__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "scop__bash-completion__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "sstephenson__bats__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "termux__termux-packages__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "tj__git-extras__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "tj__n__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "tmux-plugins__tpm__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "tteck__Proxmox__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "v1s1t0r1sh3r3__airgeddon__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "void-linux__void-packages__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
  - side: shuck-only
    path_contains: "xwmx__nb__"
    reason: "Shuck intentionally treats `for` loops that iterate over command substitution output as a line-orientation hazard, because the shell will re-split that output into separate words before the loop sees it. ShellCheck does not report this SC2013 family of cases, so these repo-family hits remain a reviewed divergence instead of expanding the rule to match ShellCheck's silence."
