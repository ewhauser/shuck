reviewed_divergences:
  - side: shuck-only
    path_contains: "233boy__v2ray__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "Bash-it__bash-it__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "CISOfy__lynis__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "GameServerManagers__LinuxGSM__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "HariSekhon__DevOps-Bash-tools__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "SlackBuildsOrg__slackbuilds__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "acmesh-official__acme.sh__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "alexanderepstein__Bash-Snippets__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "alpinelinux__aports__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "basecamp__omarchy__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "bats-core__bats-core__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "bin456789__reinstall__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "bitnami__containers__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "bittorf__kalua__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "community-scripts__ProxmoxVE__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "docker-mailserver__docker-mailserver__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "docker__docker-bench-security__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "dockur__windows__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "dokku__dokku__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "dylanaraps__neofetch__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "google__oss-fuzz__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "itzg__docker-minecraft-server__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "juewuy__ShellCrash__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "ko1nksm__shellspec__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "lmc999__RegionRestrictionCheck__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "masonr__yet-another-bench-script__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "paulirish__dotfiles__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "pi-hole__pi-hole__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "quickemu-project__quickemu__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "rvm__rvm__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "super-linter__super-linter__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "termux__termux-packages__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "tteck__Proxmox__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "v1s1t0r1sh3r3__airgeddon__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "void-linux__void-packages__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
  - side: shuck-only
    path_contains: "xwmx__nb__"
    reason: "C077 intentionally flags command substitutions nested inside arithmetic expansion, including direct `$(...)` uses and arithmetic expressions that evaluate command output first. The current ShellCheck oracle in this workspace does not emit SC2290 for that construct, so these repo-family hits remain a reviewed policy divergence rather than a shuck implementation bug."
