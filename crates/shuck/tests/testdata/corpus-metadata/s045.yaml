reviewed_divergences:
  - side: shuck-only
    path_suffix: "pyenv__pyenv__plugins__python-build__bin__python-build"
    reason: "This helper script uses arithmetic updates in generated option-parsing and version-formatting code, and the current ShellCheck oracle does not emit SC2004 for those sites even though Shuck keeps the warning for the same `$((...))` pattern elsewhere."
  - side: shuck-only
    path_suffix: "bats-core__bats-core__libexec__bats-core__bats-preprocess"
    line: 19
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "dehydrated-io__dehydrated__dehydrated"
    line: 760
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "dehydrated-io__dehydrated__dehydrated"
    line: 765
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "ohmyzsh__ohmyzsh__plugins__macos__spotify"
    line: 231
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "ohmyzsh__ohmyzsh__plugins__macos__spotify"
    line: 256
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "ohmyzsh__ohmyzsh__plugins__macos__spotify"
    line: 260
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "ohmyzsh__ohmyzsh__plugins__macos__spotify"
    line: 263
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "sstephenson__bats__libexec__bats"
    line: 68
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "sstephenson__bats__libexec__bats-preprocess"
    line: 18
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "termux__termux-packages__scripts__updates__internal__termux_github_graphql.sh"
    line: 28
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "termux__termux-packages__scripts__updates__internal__termux_github_graphql.sh"
    line: 36
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "termux__termux-packages__scripts__updates__internal__termux_github_graphql.sh"
    line: 46
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "termux__termux-packages__scripts__updates__internal__termux_github_graphql.sh"
    line: 61
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shuck-only
    path_suffix: "void-linux__void-packages__common__xbps-src__shutils__purge_distfiles.sh"
    line: 78
    reason: "The current ShellCheck oracle reuses SC2004 for bare substring and array-slice arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of slice offsets and lengths."
  - side: shellcheck-only
    path_suffix: "ohmyzsh__ohmyzsh__plugins__catimg__catimg.sh"
    line: 91
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "ohmyzsh__ohmyzsh__plugins__gitfast__git-completion.bash"
    line: 1136
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "ohmyzsh__ohmyzsh__plugins__gitfast__git-completion.bash"
    line: 1182
    reason: "The current ShellCheck oracle folds this command-style arithmetic `for ((...; ...; ...))` warning into SC2004, while Shuck keeps that double-paren context under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "ohmyzsh__ohmyzsh__plugins__gitfast__git-completion.bash"
    line: 3493
    reason: "The current ShellCheck oracle reuses SC2004 for bare array-subscript arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of subscript expressions."
  - side: shellcheck-only
    path_suffix: "ohmyzsh__ohmyzsh__plugins__gitfast__git-completion.bash"
    line: 3497
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "ohmyzsh__ohmyzsh__plugins__gitfast__git-completion.bash"
    line: 3504
    reason: "The current ShellCheck oracle reuses SC2004 for bare array-subscript arithmetic, while Shuck keeps S045 scoped to `$((...))` expansion sites instead of subscript expressions."
  - side: shellcheck-only
    path_suffix: "pyenv__pyenv__libexec__pyenv-rehash"
    line: 124
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "pyenv__pyenv__libexec__pyenv-versions"
    line: 90
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "pyenv__pyenv__libexec__pyenv-versions"
    line: 102
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "pyenv__pyenv__libexec__pyenv-versions"
    line: 143
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "pyenv__pyenv__pyenv.d__install__latest.bash"
    line: 22
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__contrib__ps1_functions"
    line: 34
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__contrib__ps1_functions"
    line: 95
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__functions__build_config"
    line: 96
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__functions__build_config"
    line: 136
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__functions__build_requirements"
    line: 101
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__functions__build_requirements"
    line: 175
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__functions__build_requirements_helpers"
    line: 186
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__functions__manage__base_install"
    line: 64
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__hash"
    line: 24
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__info"
    line: 189
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__repair"
    line: 152
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__set"
    line: 128
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__set"
    line: 157
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "rvm__rvm__scripts__set"
    line: 162
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "sstephenson__bats__libexec__bats-exec-suite"
    line: 39
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "sstephenson__bats__libexec__bats-exec-suite"
    line: 43
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "sstephenson__bats__libexec__bats-format-tap-stream"
    line: 85
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
  - side: shellcheck-only
    path_suffix: "termux__termux-packages__scripts__build__termux_step_install_service_scripts.sh"
    line: 12
    reason: "The current ShellCheck oracle folds this command-style `((...))` arithmetic warning into SC2004, while Shuck keeps command arithmetic under S048 instead of S045."
