reviewed_divergences:
  - side: shuck-only
    path_contains: "233boy__v2ray__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "CISOfy__lynis__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "SlackBuildsOrg__slackbuilds__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "alexanderepstein__Bash-Snippets__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "alpinelinux__aports__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "aristocratos__bashtop__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "awslabs__git-secrets__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "basecamp__omarchy__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "bin456789__reinstall__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "bittorf__kalua__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "community-scripts__ProxmoxVE__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "dokku__dokku__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "fideloper__Vaprobash__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "gentoo__gentoo__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "google__oss-fuzz__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "helmuthdu__aui__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "itzg__docker-minecraft-server__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "megastep__makeself__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "moovweb__gvm__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "nvm-sh__nvm__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "openrc__openrc__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "paulirish__dotfiles__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "rvm__rvm__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "sickcodes__Docker-OSX__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "spiritLHLS__one-click-installation-script__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "swoodford__aws__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "termux__termux-packages__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "tteck__Proxmox__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
  - side: shuck-only
    path_contains: "void-linux__void-packages__"
    reason: "S058 intentionally keeps a dedicated mkdir-path quoting diagnostic, while the current ShellCheck comparison target reports overlapping cases through SC2086 instead of SC2335."
