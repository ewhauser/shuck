reviewed_divergences:
  - side: shuck-only
    path_contains: "89luca89__distrobox__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "Bash-it__bash-it__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "RetroPie__RetroPie-Setup__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "SlackBuildsOrg__slackbuilds__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "acmesh-official__acme.sh__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "alexanderepstein__Bash-Snippets__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "bats-core__bats-core__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "bittorf__kalua__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "dokku__dokku__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "juewuy__ShellCrash__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "moovweb__gvm__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "nvm-sh__nvm__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "pyenv__pyenv__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "rvm__rvm__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "termux__termux-packages__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "tj__git-extras__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "v1s1t0r1sh3r3__airgeddon__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "void-linux__void-packages__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these repo-family hits remain reviewed divergence noise for parity testing."
  - side: shuck-only
    path_contains: "xwmx__nb__"
    reason: "S038 intentionally flags explicit `return $?` status propagation inside function bodies when the function already preserves the previous command's exit status. The current ShellCheck 0.11.0 comparison target does not report that pattern in the corpus, so these hits are reviewed divergence noise for parity testing."
