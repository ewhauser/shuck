reviewed_divergences:
  - side: shuck-only
    path_contains: "89luca89__distrobox__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "Bash-it__bash-it__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "CISOfy__lynis__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "HariSekhon__DevOps-Bash-tools__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "RetroPie__RetroPie-Setup__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "SlackBuildsOrg__slackbuilds__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "acmesh-official__acme.sh__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "alexanderepstein__Bash-Snippets__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "alpinelinux__aports__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "angristan__openvpn-install__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "aristocratos__bashtop__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "asdf-vm__asdf__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "awslabs__git-secrets__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "basecamp__omarchy__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "bats-core__bats-core__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "bin456789__reinstall__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "bitnami__containers__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "bittorf__kalua__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "community-scripts__ProxmoxVE__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "dehydrated-io__dehydrated__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "docker-library__official-images__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "docker-mailserver__docker-mailserver__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "dokku__dokku__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "dylanaraps__pure-bash-bible__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "gentoo__gentoo__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "google__oss-fuzz__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "helmuthdu__aui__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "itzg__docker-minecraft-server__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "ko1nksm__shellspec__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "leebaird__discover__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "moovweb__gvm__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "nvm-sh__nvm__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "p8952__bocker__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "paulirish__dotfiles__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "pi-hole__pi-hole__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "pyenv__pyenv__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "quickemu-project__quickemu__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "rbenv__rbenv__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "rvm__rvm__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "sstephenson__bats__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "termux__termux-packages__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "tj__git-extras__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "tj__n__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "tmux-plugins__tpm__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "v1s1t0r1sh3r3__airgeddon__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "v2fly__fhs-install-v2ray__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "void-linux__void-packages__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
  - side: shuck-only
    path_contains: "xwmx__nb__"
    reason: "Shuck intentionally keeps warning on `for` headers that resolve to a single item, including runtime-fixed paths built from quoted scalar expansions or command substitutions. In the pinned large-corpus oracle, SC2043 only appears for a much narrower subset of those loops, so these repo-family hits are treated as a reviewed policy divergence rather than an implementation bug."
