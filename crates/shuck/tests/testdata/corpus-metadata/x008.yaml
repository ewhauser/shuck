comparison_target_notes:
  - current_shellcheck_code: "SC3018"
    reason: "The nix-provided ShellCheck in this workspace reports standalone `(( ))` arithmetic commands as SC3006, while SC3018 is used for C-style `for ((...))` arithmetic operator warnings such as `++`."

reviewed_divergences:
  - side: shuck-only
    path_suffix: "ohmyzsh__ohmyzsh__oh-my-zsh.sh"
    labels:
      - project-closure
      - shell-collapse
    reason: "This bootstrap script returns before the `(( ))` commands when `ZSH_VERSION` is unset, so the arithmetic form is intentionally confined to the zsh-only execution path for this fixture."
