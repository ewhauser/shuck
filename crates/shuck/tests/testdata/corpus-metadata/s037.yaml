reviewed_divergences:
  - side: shuck-only
    path_suffix: "pyenv__pyenv__libexec__pyenv-rehash"
    line: 69
    column: 7
    end_line: 70
    end_column: 86
    labels:
      - directive-handling
      - helper-library
      - project-closure
    reason: "This helper-library fixture uses a continued multi-line stderr `echo` for an internal rehash message, and the current ShellCheck comparison stays silent on SC2263 for that project-closure context while shuck still flags the repeated spacing pattern."
