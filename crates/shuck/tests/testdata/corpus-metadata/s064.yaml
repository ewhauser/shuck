reviewed_divergences:
  - side: shuck-only
    path_suffix: "dokku__dokku__plugins__nginx-vhosts__functions"
    line: 406
    reason: "This helper-wrapper `xargs -i` use feeds a here-string into `echo`, and the current oracle stays silent on that wrapper shape even though Shuck still reports the deprecated short flag."
  - side: shuck-only
    path_suffix: "termux__termux-packages__packages__emscripten__build.sh"
    line: 388
    reason: "These `xargs -i bash -c` helper probes still use the deprecated short flag, but the current oracle does not emit the deprecation warning for this wrapper form."
  - side: shuck-only
    path_suffix: "termux__termux-packages__packages__emscripten__build.sh"
    line: 389
    reason: "These `xargs -i bash -c` helper probes still use the deprecated short flag, but the current oracle does not emit the deprecation warning for this wrapper form."
  - side: shuck-only
    path_suffix: "termux__termux-packages__packages__vulkan-validation-layers__build.sh"
    line: 21
    reason: "This `xargs -i bash -c` inspection helper keeps the deprecated short flag, while the current oracle stays silent on the wrapped `bash -c` form."
