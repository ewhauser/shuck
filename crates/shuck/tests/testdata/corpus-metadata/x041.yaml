reviewed_divergences:
  - side: shuck-only
    reason: "the current nix-provided ShellCheck build does not expose array-style subscripts inside `[[ ... ]]` under a stable dedicated compatibility code, so Shuck's X041 portability hits stay reviewed until the oracle has a directly comparable warning"
