---
source: crates/shuck-linter/src/rules/style/mod.rs
assertion_line: 116
---
S032 assignment hidden inside a `&&`/`||` shortcut is harder to follow than an explicit `if`
 --> <source>:3:10
  |
3 | false || remove=set
  |

S032 assignment hidden inside a `&&`/`||` shortcut is harder to follow than an explicit `if`
 --> <source>:5:9
  |
5 | true && remove=set
  |

S032 assignment hidden inside a `&&`/`||` shortcut is harder to follow than an explicit `if`
 --> <source>:7:1
  |
7 | remove=set || echo nope
  |

S032 assignment hidden inside a `&&`/`||` shortcut is harder to follow than an explicit `if`
 --> <source>:9:9
  |
9 | true && remove=set && echo later
  |
