# Getting Started

Shuck is a fast shell script linter and formatter, built in Rust.

## Installation

```bash
cargo install shuck
```

## Usage

```bash
shuck check .
```

Shuck will discover shell scripts by extension (`.sh`, `.bash`, `.zsh`, `.ksh`) or shebang and report any issues found.
